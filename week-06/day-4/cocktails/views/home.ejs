<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title>Cocktails</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" type="text/css" media="screen" href="static/style.css" />
  <!--<script src="main.js"></script>-->
</head>
<body>
  <h1>Cocktails</h1>
  <div class='alcoholSelector'>
    <a class="alcoholList" href='/'>all</a>
    <% for (let i=0; i < alcoholList.length; i++) { %>
      <a class="alcoholList" href=<%= `/?selectedAlcohol=${alcoholList[i]}`%>><%= alcoholList[i] %></a>
    <% } %>
  </div>
  <table>
    <tr>
      <th>NAME</th>
      <th>PRICE</th>
    </tr>
    <% for (let i=0; i < cocktails.length; i++) { %>
      <tr>
        <td><%= cocktails[i].name %> <% if (!cocktails[i].isAlcoholic) { %>(non-alcoholic)<% } %><br><%= `${cocktails[i].contains.join(', ')}` %></td>
        <td><%= cocktails[i].price %> Ft</td>
      </tr>
    <% } %>
  </table>
</body>
</html>